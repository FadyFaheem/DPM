#Run all API specs (RSpec)
cd api && bundle exec rspec

#Run a specific API spec file
read -p "Enter spec file (e.g., spec/models/dino_report_spec.rb): " tf && cd api && bundle exec rspec "$tf"

#Run all frontend tests
cd frontend && npx vitest run

#Run frontend tests in watch mode
cd frontend && npx vitest

#Run frontend tests with coverage
cd frontend && npx vitest run --coverage

#Run a specific frontend test file
read -p "Enter test file (e.g., client.test.ts): " tf && cd frontend && npx vitest run "$tf"

#Run ALL tests (API + Frontend)
cd api && bundle exec rspec && cd ../frontend && npx vitest run

#Install API dependencies (bundle)
cd api && bundle install

#Install frontend dependencies
cd frontend && npm install
